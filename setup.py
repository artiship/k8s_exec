from setuptools import setup

setup(name='k8s_exec',
      version='0.1',
      description='k8s exec',
      url='',
      author='lichuanliang',
      license='MIT',
      packages=['k8s_exec'],
      scripts=['bin/k8s_exec'],
      zip_safe=False)